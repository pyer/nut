3.0
Each goal calls two 'execute' methods from "Goal" and "GoalPackaging" classes

2.0
No plugins to improve speed.
simpler repository layout.
First build with install script.
Option -r to build release. Default is snapshot.
parent is a path to another nut file


1.2
plugins and dependencies use core version if version is undefined 
junit is replaced by testng

1.1
use plexus-utils-3.0.jar instead of compiling sources
add plugin release
add xml packaging
remove parent in nut.xml

1.0
Initial release
