<!-- START SNIPPET: superpom -->
<project>

  <build>
    <directory>target</directory>
    <outputDirectory>target/webapp</outputDirectory>
    <testOutputDirectory>target/test</testOutputDirectory>
    <sourceDirectory>src/webapp</sourceDirectory>
    <testSourceDirectory>test/webapp</testSourceDirectory>

    <plugins>
      <plugin>
        <groupId>nut.plugins</groupId>
        <artifactId>cleaner</artifactId>
        <goal>clean</goal>
      </plugin>
      <plugin>
        <groupId>nut.plugins</groupId>
        <artifactId>depProcessor</artifactId>
        <goal>process</goal>
      </plugin>
      <plugin>
        <groupId>nut.plugins</groupId>
        <artifactId>javaArchiver</artifactId>
        <goal>package</goal>
      </plugin>
      <plugin>
        <groupId>nut.plugins</groupId>
        <artifactId>installer</artifactId>
        <goal>install</goal>
      </plugin>
    </plugins>
  </build>

</project>
<!-- END SNIPPET: superpom -->
